name: tech-challenge

on:
  push:

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Yarn
        run: npm install -g yarn

      - name: Install Dependencies
        run: yarn --force

      - name: Copy settings file
        run: cp ./src/packages/api/config/settings.template ./src/packages/api/config/settings.json

      - name: Build project
        run: yarn build
